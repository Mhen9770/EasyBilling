spring.application.name=notification-service
server.port=8088

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/easybilling_notification?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Async Configuration
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100

# Logging
logging.level.com.easybilling.notification=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Email Configuration (TODO: Configure actual SMTP)
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=
# spring.mail.password=
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# SMS Configuration (TODO: Configure SMS provider)
# sms.provider.url=
# sms.provider.api-key=

# WhatsApp Configuration (TODO: Configure WhatsApp Business API)
# whatsapp.api.url=
# whatsapp.api.token=
